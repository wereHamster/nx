{
  "$schema": "<%= rootOffset %>node_modules/ng-packagr/ng-package.schema.json",
  "dest": <% if (isTsSolutionSetup) { %>"dist"<% } else { %>"<%= rootOffset %>dist/<%= projectRoot %>"<% } %>,
  "lib": {
    "entryFile": "src/index.ts"
  }
}
